# BatchImageResizer-forTelegramStickers
telegram's sticker bot is headache, I couldn't find a better image resizer, so i made it myself

# Important
change image size inside the code ,also path to the image folder
